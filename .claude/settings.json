{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json\ndata = json.load(sys.stdin)\npath = data.get('tool_input', {}).get('file_path', '')\nif any(x in path for x in ['data/entity', 'data/dao', 'HavenDatabase']):\n    print('MIGRATION SAFETY CHECK: This file affects the Room schema. Before continuing, verify: (1) database version incremented in HavenDatabase.kt, (2) Migration object written, (3) seed data uses INSERT OR IGNORE with correct seedVersion gating.')\n\""
          }
        ]
      }
    ]
  }
}
