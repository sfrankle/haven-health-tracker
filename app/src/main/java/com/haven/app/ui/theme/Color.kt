package com.haven.app.ui.theme

import androidx.compose.ui.graphics.Color

// Neutrals
val OffWhite = Color(0xFFF8F6F2)
val WarmGray = Color(0xFF6B6B6B)
val SoftBlack = Color(0xFF2D2D2D)
val PaleGrey = Color(0xFFEEECE8)

// Hydration — sky blue → teal → white
val HydrationBlue = Color(0xFF90CAF9)
val HydrationTeal = Color(0xFF80CBC4)
val HydrationWhite = Color(0xFFF0FAFA)

// Food — warm green → sage → mint
val FoodGreen = Color(0xFFA5C8A0)
val FoodSage = Color(0xFF8FAE8B)
val FoodMint = Color(0xFFE8F5E9)

// Sleep — lavender → indigo → dusty blue
val SleepLavender = Color(0xFFCE93D8)
val SleepIndigo = Color(0xFF9FA8DA)
val SleepDustyBlue = Color(0xFFE8EAF6)

// Emotion — blush rose → peach → warm cream
val EmotionBlush = Color(0xFFF48FB1)
val EmotionPeach = Color(0xFFFFCC80)
val EmotionCream = Color(0xFFFFF8F0)

// Symptom — warm sand → amber → pale gold
val SymptomSand = Color(0xFFD7B896)
val SymptomAmber = Color(0xFFFFCC80)
val SymptomGold = Color(0xFFFFF9E6)

// Activity — coral → dusty rose → muted violet
val ActivityCoral = Color(0xFFEF9A9A)
val ActivityRose = Color(0xFFF48FB1)
val ActivityViolet = Color(0xFFE8D5F0)

// Entry type card backgrounds — near-white tints for the Tend screen
// Pale enough to feel soft/ethereal, distinct enough to tell apart
val FoodCard = Color(0xFFE9F2E7)       // whisper of sage green
val HydrationCard = Color(0xFFE3EEF5)  // whisper of sky blue
val SleepCard = Color(0xFFEBE7F6)      // whisper of lavender
val EmotionCard = Color(0xFFF5E6ED)    // whisper of cool rose
val SymptomCard = Color(0xFFF5EDD9)    // whisper of amber
val ActivityCard = Color(0xFFF1E9F6)   // whisper of violet

// Tab: Tend — warm cream → soft peach
val TendCream = Color(0xFFFFF8F0)
val TendPeach = Color(0xFFFFE0CC)

// Tab: Trace — slate → blue-grey → near-white
val TraceSlate = Color(0xFF90A4AE)
val TraceBlueGrey = Color(0xFFB0BEC5)
val TraceNearWhite = Color(0xFFF5F7F8)

// Tab: Weave — amber → honey → gold
val WeaveAmber = Color(0xFFFFB74D)
val WeaveHoney = Color(0xFFFFCC80)
val WeaveGold = Color(0xFFFFF8E1)

// Tab: Anchor — blush → mauve → warm white
val AnchorBlush = Color(0xFFF8BBD0)
val AnchorMauve = Color(0xFFE1BEE7)
val AnchorWarmWhite = Color(0xFFFFF5F8)
